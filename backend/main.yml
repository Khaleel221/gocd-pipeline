format_version: 10
pipelines:
  backend:
    group: App-deployment
    label_template: ${COUNT}
    lock_behavior: none
    display_order: -1
    materials:
      git-0581b96:
        git: https://github.com/Khaleel221/Expense-ansible.git
        shallow_clone: false
        auto_update: true
        branch: main
    stages:
      - DEV:
          fetch_materials: true
          jobs:
            Ansible:
              timeout: 0
              tasks:
                - exec:
                    arguments:
                      - -i
                      - backend-dev.khaleel221.shop,
                      - -e
                      - ansible_user=ec2-user
                      - -e
                      - ansible_password=DevOps321
                      - -e
                      - env=dev
                      - -e
                      - role_name=backend
                      - expense.yml
                    command: ansible

      - QA:
          fetch_materials: true
          approval:
            type: success
          jobs:
            Ansible:
              timeout: 0
              tasks:
                - exec:
                    arguments:
                      - -i
                      - backend-dev.khaleel221.shop,
                      - -e
                      - ansible_user=ec2-user
                      - -e
                      - ansible_password=DevOps321
                      - -e
                      - env=dev
                      - -e
                      - role_name=backend
                      - expense.yml
                    command: ansible-plybook
                    run_if: passed

      - STAGE:
          fetch_materials: true
          approval:
            type: success
          jobs:
            Ansible:
              timeout: 0
              tasks:
                - exec:
                    arguments:
                      - -i
                      - backend-dev.khaleel221.shop,
                      - -e
                      - ansible_user=ec2-user
                      - -e
                      - ansible_password=DevOps321
                      - -e
                      - env=dev
                      - -e
                      - role_name=backend
                      - expense.yml
                    command: ansible
                    run_if: passed

      - PROD:
          fetch_materials: true
          approval:
            type: success
          jobs:
            Ansible:
              timeout: 0
              tasks:
                - exec:
                    arguments:
                      - -i
                      - backend-dev.khaleel221.shop,
                      - -e
                      - ansible_user=ec2-user
                      - -e
                      - ansible_password=DevOps321
                      - -e
                      - env=dev
                      - -e
                      - role_name=backend
                      - expense.yml
                    command: ansible
                    run_if: passed